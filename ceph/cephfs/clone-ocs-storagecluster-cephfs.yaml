apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-clone-from-snap-calibrate-1
  namespace: test-calibrate-100k-10k
spec:
  storageClassName: ocs-storagecluster-cephfs
  dataSource:
    name: snap-calibrate-1
    kind: VolumeSnapshot
    apiGroup: snapshot.storage.k8s.io
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 32Gi